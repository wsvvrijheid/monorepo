{
  "name": "dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ../../.env.local next dev -- -p 3000",
    "dashboard": "dotenv -e ../../.env.local next dev -- -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "clean": "rm -rf node_modules .turbo .next",
    "lint": "next lint"
  }
}
