{
  "name": "lotus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ../../.env.local next dev -- -p 3003",
    "lotus": "dotenv -e ../../.env.local next dev -- -p 3003",
    "build": "next build",
    "start": "next start -p 3003",
    "clean": "rm -rf node_modules .turbo .next",
    "lint": "next lint"
  }
}
